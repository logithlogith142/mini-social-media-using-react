import React from "react";
import PropTypes from "prop-types";

const Header = () => {
  return (
    <div className="Header">
      <h1>Goodiee</h1>
    </div>
  );
};

export default Header;
